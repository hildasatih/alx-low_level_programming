#include "main.h"

/**
 * get_endianness - checks if a machine is big or little endianness
 * Return: returns for big 0, for little 1 
 */
int get_endianness(void)
{
	unsigned int x = 1;
	char *a = (char *) ax;

	return (*a);
}
